@page
@model Server.Pages.FileManager.Step03.CreateDirectoryModel7

@{
	Layout = "Layouts/_LayoutFileManager";
	ViewData["PageTitle"] = "Create Directory (7)";
}

<form method="post">

	<div class="row">
		<div class="col-12 p-3 col-md-8 offset-md-2 p-md-5 bg-light border border-2 rounded-3 shadow-lg">
			<legend>
				Create Directory
			</legend>

			<hr />

			<partial name="PartialViews/_DisplayPageMessages" />

			<div class="mb-3">
				<label asp-for="ViewModel.DirectoryName" class="form-label"></label>
				<input asp-for="ViewModel.DirectoryName" class="form-control" />
				<span asp-validation-for="ViewModel.DirectoryName" class="text-danger"></span>
			</div>

			<button type="submit" class="btn btn-primary">Create</button>
			<button type="reset" class="btn btn-secondary">Reset</button>
		</div>
	</div>

</form>

<hr />

<button type="reset" class="btn btn-primary">Outside Reset</button>
<button type="button" class="btn btn-primary">Outside Button</button>
<button type="submit" class="btn btn-primary">Outside Submit</button>

@section Scripts
{
	<script>

		// $(document).ready(function()
		// {
		// 	console.log("ready (1)!");
		// });

		// $(document).ready(function() {
		// 	console.log("ready (2)!");
		// });

		// $(document).ready(function() {

		// 	console.log("ready (3)!");

		// });

		// $(document).ready(function() {

		// 	console.log("ready (4)!")

		// })

		// $(function() {

		// 	console.log("ready (5)!")

		// })

		$(function() {

			$('form').submit(function(e) {

				if ($(this).valid()) {

					// JQuery در نسخه‌های جدید
					$('button').prop('disabled', true)
					$('input[type=reset]').prop('disabled', true)
					$('input[type=submit]').prop('disabled', true)
					$('input[type=button]').prop('disabled', true)

					// JQuery در نسخه‌های قدیمی
					//$('button').attr('disabled', 'disabled')
					// $('input[type=reset]').attr('disabled', 'disabled')
					// $('input[type=submit]').attr('disabled', 'disabled')
					// $('input[type=button]').attr('disabled', 'disabled')

				}

			})

		})

	</script>
}
